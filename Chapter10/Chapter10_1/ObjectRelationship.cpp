#include <iostream>

int main()
{
	// 객체들의 관계는
	// 구성(요소), 집합, 제휴(연계), 의존, 상속 5개가 있다
	// Composition, Aggregation, Association, Dependency, Inheritance
	// part-of, has-a, uses-a, depends-on, is-a

	return 0;
}